server.port=${PORT:8081}
groq.api.key=${GROQ_API_KEY}

# Fylls av Maven vid bygget (@project.version@) respektive av Render vid deploy
# (RENDER_GIT_COMMIT/RENDER_GIT_BRANCH). Lokalt ar de tva sistnamnda tomma.
# Tillkom 2026-09-17: modellbytet qwen3.6 -> 3.8 gick inte att verifiera i drift, for
# tjansten hade inget satt att saga vilken kod den kor. Samma endpoint som CarAdvice har.
app.version=@project.version@
app.commit=${RENDER_GIT_COMMIT:}
app.branch=${RENDER_GIT_BRANCH:}
